pipeline {
    agent { label 'slave' }
    stages { 
        stage('Clone') {
            steps {
             sh 'git clone https://github.com/Prabandh/hello-world-war.git'
            }
        }
    }
}
